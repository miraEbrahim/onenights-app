import React from 'react';
import Map from '../Map/Map';

const PlacesMap = () => (
  <div>
    <h1>places PlacesMap</h1>
    <p>The Home Page is accessible by every signed in user.</p>
    <Map />
  </div>
);

export default PlacesMap;
